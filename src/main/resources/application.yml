##spring.application.name=products

## Configuración de Spring
spring:
 application:
  name: ms-inventory-products
 datasource:
  url: jdbc:h2:mem:testdb
 jpa:
  show-sql: true
 h2:
  console:
   enabled: true
   path: /h2-console
   settings:
    web-allow-otrhers: true
## Configuración de Instancia
server:
 port: 8088